apiVersion: v1
kind: Service
metadata:
  name: {{ include "client-manager.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "client-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "client-manager.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.config.server.port }}
      protocol: TCP